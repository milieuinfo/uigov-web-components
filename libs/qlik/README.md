# qlik

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build qlik` to build the library.

## Running unit tests

Run `nx test qlik` to execute the unit tests via [Jest](https://jestjs.io).
