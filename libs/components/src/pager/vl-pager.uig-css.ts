import { css, CSSResult } from 'lit';

const styles: CSSResult = css`
    [hidden] {
        display: none;
    }

    a {
        cursor: pointer;
    }

    a .vl-link__icon {
        position: relative;
    }
`;
export default styles;
