import { html } from 'lit-html';
import { classMap } from 'lit/directives/class-map.js';

export default {
    id: 'opmaak-next-native-body',
    title: 'Opmaak-next/native/body',
    tags: ['autodocs'],
};

export const body = ({}) => html` <div class=${classMap({})}>
    <div>TODO</div>
</div>`;
body.storyName = 'vl-body - todo';
