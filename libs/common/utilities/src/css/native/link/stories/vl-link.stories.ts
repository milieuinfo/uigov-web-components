import { html } from 'lit-html';
import { classMap } from 'lit/directives/class-map.js';

export default {
    id: 'opmaak-next-native-link',
    title: 'Opmaak-next/native/link',
    tags: ['autodocs'],
};

export const link = ({}) => html` <div class=${classMap({})}>
    <div>TODO</div>
</div>`;
link.storyName = 'vl-link - todo';
