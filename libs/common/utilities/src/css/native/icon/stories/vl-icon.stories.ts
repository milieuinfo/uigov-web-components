import { html } from 'lit-html';
import { classMap } from 'lit/directives/class-map.js';

export default {
    id: 'opmaak-next-native-icon',
    title: 'Opmaak-next/native/icon',
    tags: ['autodocs'],
};

export const icon = ({}) => html` <div class=${classMap({})}>
    <div>TODO</div>
</div>`;
icon.storyName = 'vl-icon - todo';
