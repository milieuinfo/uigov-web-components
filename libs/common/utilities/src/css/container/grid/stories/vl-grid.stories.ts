import { html } from 'lit-html';
import { classMap } from 'lit/directives/class-map.js';

export default {
    id: 'opmaak-next-container-grid',
    title: 'Opmaak-next/container/grid',
    tags: ['autodocs'],
};

export const grid = ({}) => html` <div class=${classMap({})}>
    <div>TODO</div>
</div>`;
grid.storyName = 'vl-grid - todo';
