import { html } from 'lit-html';
import { classMap } from 'lit/directives/class-map.js';

export default {
    id: 'opmaak-next-container-padding',
    title: 'Opmaak-next/container/padding',
    tags: ['autodocs'],
};

export const padding = ({}) => html` <div class=${classMap({})}>
    <div>TODO</div>
</div>`;
padding.storyName = 'vl-padding - todo';
