
# Click Action

Capteert een klikactie op een map. Plaatst een pindrop op de kaart en vuurt een event af die kan worden opgevangen in de onClick callback.

## Voorbeeld

TODO: check
```js
import { VlMapClickAction } from '@domg-lib/components'
```

```html
        <vl-map>
            <vl-map-baselayer-grb-gray></vl-map-baselayer-grb-gray>
            <vl-map-click-action onClick="${(click) => {onClick(click)}}"></vl-map-click-action>
        </vl-map>
```
