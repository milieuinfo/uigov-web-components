import { ArgsTable, DocsStory, PRIMARY_STORY } from '@storybook/addon-docs';

# Map Action Controls

Gebruik het `map-action-controls` component als wrapper rond [map-action-control](/docs/map-controls-action-control--map-action-control-default) componenten.<br/>
De `map-action-controls` zorgt ervoor dat de map-acties in de rechterbovenhoek van de `map` afgebeeld worden.<br/>
Verder zorgt deze ervoor dat er altijd maar 1 actieve `map-action-control` is.

## Voorbeeld

```js
import { VlMapActionControls } from '@domg-wc/map';
```

```html
<vl-map-action-controls></vl-map-action-controls>
```

<DocsStory id="map-controls-action-controls--map-action-controls-default" />

## Configuratie

Dit component heeft geen configuratie.

## Referenties

### Legacy Documentatie

**Legacy Storybook:** https://uig-webcomponents.omgeving.vlaanderen.be/?path=/docs/custom-elements-vl-map-vl-map-action-controls--default
