import { ArgTypes, Canvas } from '@storybook/blocks';
import * as VlMapTiledWmsLayerStories from './vl-map-tiled-wms-layer.stories';

# Map Tiled WMS Layer

<CustomDocMetaData id="map-layer-wms-layer-tiled-wms-layer" />

Gebruik de `map-tiled-wms-layer` component om een WMS kaartlaag af te beelden.<br/>
Deze component erft over van de `VlMapWmsLayer` klasse, die op zijn beurt overerft van de `VlMapLayer` klasse.


## Voorbeeld

```js
import { VlMapTiledWmsLayer } from '@domg-wc/map';
```

```html
<vl-map-tiled-wms-layer></vl-map-tiled-wms-layer>
```

<Canvas of={VlMapTiledWmsLayerStories.MapTiledWmsLayerDefault} />


## Configuratie

<ArgTypes of={VlMapTiledWmsLayerStories.MapTiledWmsLayerDefault} />


## Referenties

### Legacy Documentatie

[Legacy Storybook - Map Tiled Wms Layer](https://webcomponenten.omgeving.vlaanderen.be/storybook/?path=/docs/custom-elements-vl-map-vl-map-tiled-wms-layer--default)

[Legacy Documentatie - Map Tiled Wms Layer](https://webcomponenten.omgeving.vlaanderen.be/doc/VlMapTiledWmsLayer.html)
