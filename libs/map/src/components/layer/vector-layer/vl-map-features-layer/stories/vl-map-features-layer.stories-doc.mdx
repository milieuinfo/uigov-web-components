import { ArgTypes, Canvas } from '@storybook/blocks';
import * as VlMapFeaturesLayerStories from './vl-map-features-layer.stories';

# Map Features Layer

<CustomDocMetaData id="map-layer-vector-layer-features-layer" />

Gebruik de `map-features-layer` component om een kaartlaag af te beelden waarbij je een set van te tonen features kan
stellen.<br/>
Deze component erft over van de `VlMapVectorLayer` klasse, die op zijn beurt overerft van de `VlMapLayer` klasse.


## Voorbeeld

```js
import { VlMapFeaturesLayer } from '@domg-wc/map';
```

```html
<vl-map-features-layer></vl-map-features-layer>
```

<Canvas of={VlMapFeaturesLayerStories.MapFeaturesLayerDefault} />


## Configuratie

<ArgTypes of={VlMapFeaturesLayerStories.MapFeaturesLayerDefault} />


## Referenties

### Legacy Documentatie

[Legacy Storybook - Map Features Layer](https://webcomponenten.omgeving.vlaanderen.be/storybook/?path=/docs/custom-elements-vl-map-vl-map-features-layer--default)

[Legacy Documentatie - Map Features Layer](https://webcomponenten.omgeving.vlaanderen.be/doc/VlMapFeaturesLayer.html)
