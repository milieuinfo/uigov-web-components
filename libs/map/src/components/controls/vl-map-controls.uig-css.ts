import { css, CSSResult } from 'lit';

const styles: CSSResult = css`
    div {
        position: absolute;
        right: 0px;
        z-index: 1;
        display: flex;
        column-gap: 10px;
        padding: 10px;
    }
`;
export default styles;
