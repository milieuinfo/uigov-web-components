import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Beheren/Icon Font"/>

# Icon Font

## Inhoudstafel

- [Beschrijving](#beschrijving)
- [Legacy opzet](#oude-opzet)
- [V2 opzet](#v2-opzet)
- [Het font updaten](#het-font-updaten)


## Beschrijving

Het icon-font is deel van de flux-assets repository en wordt geversioneerd gepubliceerd op de CDN van Departement
Omgeving.<br/>
De flux-web-components refereren intern naar dit font via CSS-classes die een mapping voorzien tussen de icoon-naam
en de unicode waarde van het icoon in het font.

## Legacy opzet

In de oude opzet bevatten alle componenten die een icoon nodig hebben de icon-mapping CSS-classes. Dit zorgt voor
veel duplicatie van CSS code.

## v2 opzet

Er is een centrale plaats in de flux-web-components repository waar de icon-mapping CSS-classes gedefinieerd worden
als CSS-in-JS. Elk component dat een icoon nodig heeft importeert dit icon-mapping bestand. Er is geen duplicatie
van CSS omdat overal dezelfde instantie van het CSS-in-JS object gebruikt wordt. Dit icon-mapping bestand wordt
gegenereerd m.b.v. het script `resources/generate-icons/generate-icon-files.ts`.

## Het font updaten

Volg de volgende stappen om het icon-font te updaten:
- plaats de nieuwe versie van het font in de flux-assets repository
- deploy de nieuwe versie van het font geversioneerd op de CDN van Departement Omgeving
- refereer in de flux-web-components repository naar de nieuwe versie van het font
- voer het `generate-icon-files.ts` script uit in de flux-web-components repository (`pnpm run libs:icons:generate`)
- verifieer dat de icon-mapping correct is gegenereerd met behulp van de
  [icon pagina](/story/components-icon--all-icons)
