import { css, CSSResult } from 'lit';

const styles: CSSResult = css`
    section.form-data {
        display: flex;
        flex-direction: column;
        gap: 1rem;
    }
`;
export default styles;
