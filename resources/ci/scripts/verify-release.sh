#!/bin/bash

# exit on error
set -e

echo 'RUNNING SCRIPT: verify-release.sh'

# TODO
# in de consumer-app de versies in package.json verhogen naar de release versie + npm install
# voor de fat-js: de versie afhalen en via een lokale serve testen
#echo "TODO - run the consumer e2e tests"
#npm run consumer:install
#npm run consumer:run

